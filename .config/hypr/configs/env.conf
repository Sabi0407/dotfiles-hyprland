

################################################################################
###                      VARIABLES D'ENVIRONNEMENT                          ###
################################################################################
# Configuration des variables d'environnement, programmes par défaut et thèmes
# Documentation : https://wiki.hyprland.org/Configuring/Environment-variables/
################################################################################

# ═══════════════════════════════════════════════════════════════════════════════
# SUPPORT CARTES GRAPHIQUES NVIDIA (décommenter si nécessaire)
# ═══════════════════════════════════════════════════════════════════════════════
#env = WLR_NO_HARDWARE_CURSOR,1               # Désactive le curseur matériel
#env = LIBVA_DRIVER_NAME,nvidia               # Pilote NVIDIA pour VA-API
#env = GBM_BACKEND,nvidia-drm                 # Backend GBM pour NVIDIA
#env = __GLX_VENDOR_LIBRARY_NAME,nvidia       # Bibliothèque GLX de NVIDIA
#env = WLR_RENDERER_ALLOW_SOFTWARE,1          # Autorise le rendu logiciel

# ═══════════════════════════════════════════════════════════════════════════════
# CONFIGURATION DES THÈMES QT ET GTK
# ═══════════════════════════════════════════════════════════════════════════════

# Thème Qt moderne (Qt6)
env = QT_QPA_PLATFORMTHEME,qt6ct
env = QT_STYLE_OVERRIDE,kvantum
env = QT_QPA_PLATFORM,wayland

# Support Qt5 pour KDE Connect et applications Qt5
env = QT_QPA_PLATFORMTHEME,qt5ct
env = QT5_STYLE_OVERRIDE,kvantum-dark

# Thème GTK avec mode sombre et décorations masquées
env = GTK_THEME,catppuccin-mocha-red-standard+default
env = GTK_APPLICATION_PREFER_DARK_THEME,1
env = GTK_CSD,0
env = GTK_TITLEBAR,0

# Cache Pywal dans ~/.config pour centraliser les thèmes
env = PYWAL_CACHE_DIR,/home/sabi/.config/wal/cache

# ═══════════════════════════════════════════════════════════════════════════════
# CONFIGURATION DES CURSEURS
# ═══════════════════════════════════════════════════════════════════════════════
env = HYPRCURSOR_THEME,catppuccin-mocha-red-cursors
env = HYPRCURSOR_SIZE,24
env = XCURSOR_THEME,catppuccin-mocha-red-cursors
env = XCURSOR_SIZE,24

# ═══════════════════════════════════════════════════════════════════════════════
# LOCALISATION ET LANGUE
# ═══════════════════════════════════════════════════════════════════════════════
env = LANG,fr_FR.UTF-8
env = LC_TIME,fr_FR.UTF-8

# ═══════════════════════════════════════════════════════════════════════════════
#  VARIABLES HYPRLAND PRINCIPALES
# ═══════════════════════════════════════════════════════════════════════════════
$mainMod = SUPER                    # Touche Windows comme modificateur principal
$terminal = kitty
$fileManager = thunar
$menu = tofi-drun
env = TERMINAL,kitty

# ═══════════════════════════════════════════════════════════════════════════════
#  APPLICATIONS SPÉCIFIQUES
# ═══════════════════════════════════════════════════════════════════════════════

# Configuration pour Packet Tracer (utilise XCB au lieu de Wayland)
env = QT_QPA_PLATFORM=xcb packettracer

# Firefox - Optimisations Wayland et décorations
env = MOZ_ENABLE_WAYLAND,1
env = MOZ_WEBRENDER,1
env = MOZ_USE_XINPUT2,1

# Capture d'écran Wayland (compatibilité PipeWire)
env = WLR_DRM_NO_MODIFIERS,1              # Force des buffers sans modificateurs
